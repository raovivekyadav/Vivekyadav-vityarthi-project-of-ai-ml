# Vivekyadav-vityarthi-project-of-ai-ml
A disease prediction model is a supervised machine learning system that predicts diseases based on health parameters like BMI, heart rate, age, and medical test results. It is trained on labeled data and uses algorithms such as Logistic Regression, Random Forest, or SVM. 
